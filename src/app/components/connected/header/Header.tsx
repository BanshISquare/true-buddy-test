import * as React from 'react';

const Header: React.FunctionComponent<{}> = (): JSX.Element => (
	<div className="connected-header">
		<div className="container-fluid connected-header_content" />
	</div>
);

export default Header;
